Hey Jeff — need your steer on CORE-1592 before I go further. The CloudFront CDN over the content bucket is built, serving, and correctly encrypted (fixed an SSE-KMS issue that was blocking it). One open item: a security finding that the distribution's TLS floor is too old (TLSv1).

The catch: the only way to raise that floor to the modern policy (TLSv1.2_2021, which also gives TLS 1.3) is to put a custom domain + custom ACM cert on the distribution — the default cloudfront.net cert is locked to TLSv1. I've got the Terraform written for that, but it assumes we self-provision the cert via ACM DNS-validation and own the Route53 zone, and I don't think that's how we do domains/certs here.

So before I apply anything, a few calls I think are yours:
• Certs — do public certs come through the internal process (PCAM/central), not self-service ACM? If so I'd point Terraform at that cert instead of minting one.
• Domain/DNS — what's the sanctioned hostname for this, and is the authoritative DNS in Route53 or Cloudflare? (If it's Cloudflare, the whole approach changes.)
• Approach — is the right remediation a CloudFront custom domain, or do we front this through Cloudflare so it owns the domain + TLS (no cert for us to provision)? Or, since it's a dev bucket of public images, do we risk-accept the finding for now and sort the domain/cert properly later?

Not blocked on the CDN itself — just don't want to self-mint a cert / create public DNS for a domain we may not own. Happy to grab 15 min if easier.
