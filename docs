Hey Jeff — need your steer on the last open item for CORE-1592 before I apply anything. The CloudFront CDN over the content bucket is built, serving, and correctly encrypted. The one finding left is the TLS floor being too old (TLSv1) on the distribution.

To raise it to the modern policy (TLSv1.2_2021, which also gives TLS 1.3), the distribution needs a custom domain + custom cert — the default cloudfront.net cert is locked to TLSv1. I've confirmed our Route53 zone (dev.one-pass-dev.com) is real and delegated, so DNS isn't the blocker. But I noticed pass-utilization in that same zone is set up with Cloudflare + ACME-issued certs — which makes me think self-minting an ACM cert on CloudFront isn't how we do it here.

So two calls I think are yours:
• Certs — do we issue via the Cloudflare/ACME path like pass-utilization, or is a direct ACM cert on CloudFront fine for this?
• Approach — should this asset domain sit behind Cloudflare (so it owns the domain + TLS, nothing for me to mint), or stay a standalone CloudFront custom domain? Or, since it's a dev bucket of public images, do we just risk-accept the TLS finding for now and sort the domain/cert properly later?

Not blocked on the CDN itself — just don't want to stand up a cert/domain in a way that cuts against how we already do edge here. Happy to grab 15 min if easier.
